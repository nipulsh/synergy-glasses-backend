fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=1.26
opencv-python-headless>=4.9
pydantic>=2.6
python-multipart>=0.0.9
# TFLite (optional): `pip install tflite-runtime` on Python 3.10–3.12 if you add models/model.tflite.
# Python 3.13+ often has no wheel — inference.py falls back to OpenCV Sobel without it.
