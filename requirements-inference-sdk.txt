# Same runtime deps as the app. Roboflow is called over HTTP (stdlib); no inference-sdk needed.
# inference-sdk does not publish wheels for Python 3.13+ as of early 2026.
# Optional on Python 3.9–3.12 only: pip install inference-sdk
-r requirements.txt
